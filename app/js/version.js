/* Pinned shell version. Must match `const VERSION` in sw.js.
   _check.mjs fails the build if they drift. This names the Offline only
   cache. Boot does not fill it. Bump both together when that cache should
   refresh. */
export const APP_VERSION = 'rsabc-shell-v46-lucy-clips';
export const APP_LABEL = '2026.09.18 Lucy Omni idle and HeyGen talk';
