/* Pinned shell version. Must match `const VERSION` in sw.js.
   _check.mjs fails the build if they drift. Bump both together when the
   teacher-facing "Get update" cache should refresh. */
export const APP_VERSION = 'rsabc-shell-v25-az-art';
export const APP_LABEL = '2026.09.08 Drawn pictures for Q, R, S and T — A–T play on flat SVG stickers, U–Z stay on their emoji plates';
