Origin: `http://127.0.0.1:4173`. Pin `rsabc-shell-v46-lucy-clips`. Viewport 1280×800. Smoke `_smoke.html?reset=1` then Ava, then `_smoke.html?kid=k01`. Unregistered the v46 cache before the talk sample so disk `lucy.js` loaded.

`body[data-screen]=faces`. Idle `art/lucy/idle.mp4` 1280×720 muted playing. Talk `art/lucy/talk.mp4` 1080×1080 muted paused. `.has-loop` true. No page scroll. Heading font Comfortaa. Header still has Music, Sound effects, and Lucy voice.

`body[data-screen]=home`. Heading `Welcome back, Ava!`. Same idle/talk files. Talk opacity 0 until `Talk with Lucy`.

Talk sample (click paw, read the next frames): `dataset.talking=true`, talk opacity 1 and playing, idle opacity 0, both `muted`. Dialog heading `Lucy says`. Close is `Close Lucy`.

`body[data-screen]=meet` after `Play letter P`. Idle playing muted. Talk paused muted. Footer still `rsabc-shell-v46-lucy-clips`. Kid `k01`. No scroll.

Doctor OK on pid that owned 4173. `_check.mjs` ALL-OK. `_flow.mjs` ALL-OK.
